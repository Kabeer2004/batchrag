typer[all]>=0.9.0,<1.0.0
requests>=2.31.0,<3.0.0
chromadb>=0.4.24,<0.5.0
unstructured[local-inference]>=0.13.0,<0.14.0 # For diverse file types
rich>=13.0.0,<14.0.0 # For better CLI output formatting
numpy~=1.26.0
pdfminer.six==20221105
# We don't need the 'ollama' python package if using requests directly